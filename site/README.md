# Site notes

Everything in this directory is about one particular computer. The
suite does not read any of it; it exists so that knowledge about a
machine — where things are installed, how to get a display, what the
scheduler needs — is written down once and is visibly separate from
the portable parts. Add a directory per site.
